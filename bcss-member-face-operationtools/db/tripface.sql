USE TRIP_FACE;
CREATE TABLE IF NOT EXISTS `bcss_mem_record_info_log`(
   `SEQ_ID` bigint UNSIGNED AUTO_INCREMENT,
   `CORP_ID` CHAR(10) NOT NULL,
   `TRXSEQNO` VARCHAR(20) NOT NULL,
   `INFO_TYPE` VARCHAR(40) NOT NULL,
    `PERSON_ID` VARCHAR(20) NOT NULL, 
    `SEQNO` VARCHAR(10) NOT NULL, 
	`FACE_ID` VARCHAR(255) NOT NULL, 
     `RECORD_DATE` VARCHAR(20) NOT NULL, 
      `RECORD_TIME` VARCHAR(20) NOT NULL, 
      `MESSAGE` VARCHAR(100), 
      `BUSINESS_NO1` VARCHAR(100), 
      `BUSINESS_NO2` VARCHAR(100) , 
      `SORT` VARCHAR(40) , 
      `RESULT_DESC` VARCHAR(100) , 
      `MEMCARDNO` VARCHAR(30) , 
      `MEMNAME` VARCHAR(60) , 
      `INDUSTRY` VARCHAR(10) , 
      `DEAL_STATUS` VARCHAR(40), 
      `DEAL_DESC` VARCHAR(40), 
      `BACKUP1` VARCHAR(50), 
      `BACKUP2` VARCHAR(50), 
      `BACKUP3` VARCHAR(50), 
      `BACKUP4` VARCHAR(50), 
      `BACKUP5` VARCHAR(50), 
      `BACKUP6` VARCHAR(50), 
      `SETUSER` VARCHAR(50), 
      `SETDATE` VARCHAR(50), 
      `LSTMUSER` VARCHAR(40), 
      `LSTMDATE` TIMESTAMP NOT NULL, 
      `INFO_CONTENT` longblob,
   PRIMARY KEY ( `SEQ_ID` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE IF NOT EXISTS `trip_face_detail`(
   `sn_id` BIGINT UNSIGNED AUTO_INCREMENT,
   `corp_id` VARCHAR(10) NOT NULL,
   `mem_no` VARCHAR(30) NOT NULL,
   `mem_name` VARCHAR(60) NOT NULL,
   `industry` VARCHAR(10) NOT NULL,
   `person_id` VARCHAR(20) NOT NULL,
   `face_id` VARCHAR(255) NOT NULL, 
   `face_seq_no` VARCHAR(10) NOT NULL,
   `mem_path` VARCHAR(200) NOT NULL,
   `mem_detail1` VARCHAR(100) NOT NULL,
   `mem_detail2` VARCHAR(100) NOT NULL,
   `mem_detail3` VARCHAR(100) NOT NULL,
   `LSTMDATE` TIMESTAMP NOT NULL, 
    `memParam` longblob,
   PRIMARY KEY ( `sn_id` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;